apiVersion: v1
kind: Service
metadata:
  name: app
  namespace: m-platform-test
  labels:
    app: app
spec:
  ports:
    - port: 3930
      protocol: TCP
      targetPort:3301
  selector:
    app: app
  sessionAffinity: None
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app
  namespace: m-platform-test
  labels:
    app: app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app
  template:
    metadata:
      labels:
        app: app
    spec:
      containers:
      - name: app
        image: IMAGE
        ports:
        - containerPort: 3301
